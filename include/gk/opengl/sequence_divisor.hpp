#pragma once


namespace gk {
  namespace opengl {

    template<typename ... TListTypes>
    class sequence_divisor
    {
      /** @todo to cut primitives_heap on an sub divisor
       *    - seems ok for : glDrawArraysInstanced(GL_TRIANGLE_STRIP, 0, 4, ParticlesCount);
       *    - check for : glDrawElementsInstanced
       */
    };

  }
}
